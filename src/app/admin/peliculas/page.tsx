import { MovieList } from '@/components/admin/peliculas/MovieList';

const PeliculasPage = () => {
    return (
        <div>
            <MovieList />
        </div>
    );
};

export default PeliculasPage;
